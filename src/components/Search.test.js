test('Contains an input search field', () => {

})

test('Contains a button with the text "Search"', () => {

})

test('Triggers the error message when no data was entered in the search box and the search button was clicked', () => {

})

test('Sets the login value when data was entered in the search box and the search button was clicked', () => {

})